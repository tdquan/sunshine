<div class="clearfix">
  <% unless can? :show, current_user.solar_panel %>
    <p class="contracts"><%= t('.title') %></p>
    <%= link_to(t('.link_new_panel'), new_solar_panel_path, class: "navbar-wagon-button-orange") %>
  <% end %>

<% if can? :show, current_user.solar_panel %>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="card">
        <div class="text-center">
         <% if user_signed_in? && current_user.facebook_picture_url %>
            <%= image_tag current_user.facebook_picture_url, class: "avatar-large"%>
          <% else %>
            <%= image_tag "avatar.png", {alt: "avatar img",class: "avatar-large"} %>
          <% end %>
        </div>
        <br>
        <div class="card-content-container text-left">
          <p><b><%= t('.owner') %></b>&nbsp;<span><%= @solar_panel.user.first_name.capitalize + " " + @solar_panel.user.last_name.capitalize %></span></p>
          <p><b><%= t('.address') %></b>&nbsp;<span><%= @solar_panel.address %></span></p>
          <p><b><%= t('.production') %></b>&nbsp;<span><%= @solar_panel.size || 0 %></span>&nbsp;sol/hour</p>
          <p><b><%= t('.price') %></b>&nbsp;<span><%=t('.currency')%><%= @solar_panel.price || 0 %>/sol</span></p>
        </div>
        <br>
        <div class="text-center">
          <%= link_to(t('.link_to_details'), solar_panel_path(@solar_panel), class: "navbar-wagon-button-orange") %>
        </div>
      </div>
    </div>
  <% end %>

</div>

<%= link_to(t('.link_back'), dashboard_path, class: "navbar-wagon-button-orange back-btn") %>
